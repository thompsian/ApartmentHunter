import React from "react"
import PropTypes from "prop-types"
class Entry extends React.Component {
  render () {
    return (
      <React.Fragment>
        <p>Create a new Apartment listing</p>
      </React.Fragment>
    );
  }
}

export default Entry
