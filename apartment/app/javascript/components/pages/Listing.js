import React from "react"
import PropTypes from "prop-types"
class Listing extends React.Component {
  render () {
    return (
      <React.Fragment>
        <p>This is where apartment listings will show</p>
      </React.Fragment>
    );
  }
}

export default Listing
